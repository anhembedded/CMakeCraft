# {{PROJECT_NAME}} 🚀

A professional C++ module generated by **CMakeCraft**.

## 🏗 Structure

- `interface/`: Public headers (API).
- `inc/`: Private headers.
- `src/`: Implementation files.
- `tests/`: Unit tests using GoogleTest.
- `examples/`: Usage examples.
- `script/`: Automation scripts.

## ⚡ Quick Start (Automation)

The module includes an `auto_run.ps1` script to automate all common tasks.

### 🔨 Build & Test
```powershell
.\script\auto_run.ps1
```
*(Runs Clean, Configure, Build, and Tests including Examples)*

### 🎨 Format Code
```powershell
.\script\auto_run.ps1 -Format
```
*(Uses `.clang-format` based on Google Style)*

### 🔍 Static Analysis
```powershell
.\script\auto_run.ps1 -Lint
```
*(Uses `.clang-tidy` with production-ready checks)*

### 📊 Code Coverage
```powershell
.\script\auto_run.ps1 -Coverage
```
*(Generates HTML report in `coverage_report/` - Linux/WSL recommended)*

## 🛠 Manual Build

If you prefer using CMake directly:

```powershell
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build
ctest --test-dir build
```

## 🤖 CI/CD

This module is pre-configured with **GitHub Actions**. Simply push to GitHub to trigger the automated Build, Test, and Lint pipeline.

## 📄 License
MIT
